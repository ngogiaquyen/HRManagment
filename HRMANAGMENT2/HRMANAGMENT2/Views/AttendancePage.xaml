<UserControl x:Class="HRMANAGMENT2.Views.AttendancePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Controls -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="30,10,20,10" Background="#F8F9FA">
            <Label Content="Tìm kiếm:" VerticalAlignment="Center" Margin="5" FontWeight="Bold" Foreground="#495057"/>
            <TextBox x:Name="TxtSearchAttendance" Width="250" Margin="5" TextChanged="TxtSearchAttendance_TextChanged" VerticalContentAlignment="Center"/>
            <Button Content="🔍 Tìm" Click="BtnSearchAttendance_Click" Margin="5" Padding="12,8" Width="90" Background="#007BFF" Foreground="White" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0056b3"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="➕ Thêm" Click="BtnAddAttendance_Click" Margin="5" Padding="12,8" Width="90" Background="#28A745" Foreground="White" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#218838"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="✏️ Sửa" Click="BtnUpdateAttendance_Click" Margin="5" Padding="12,8" Width="90" Background="#FFC107" Foreground="Black" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#e0a800"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="🗑️ Xóa" Click="BtnDeleteAttendance_Click" Margin="5" Padding="12,8" Width="90" Background="#DC3545" Foreground="White" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#c82333"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="📊 Xuất Excel" Click="BtnExportAttendance_Click" Margin="5" Padding="12,8" Width="120" Background="#6F42C1" Foreground="White" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5a32a3"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <!-- DataGrid -->
        <ScrollViewer Grid.Row="1" Margin="20" VerticalScrollBarVisibility="Auto">
            <DataGrid x:Name="dgAttendance" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single"
                      GridLinesVisibility="Horizontal" AlternatingRowBackground="LightGray" HeadersVisibility="Column"
                      MouseDoubleClick="dgAttendance_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã Chấm Công" Binding="{Binding AttendanceId}" Width="120"/>
                    <DataGridTextColumn Header="Thông Tin NV" Binding="{Binding EmployeeInfo}" Width="*"/>
                    <DataGridTextColumn Header="Ngày Chấm Công" Binding="{Binding AttendanceDate, StringFormat=dd/MM/yyyy}" Width="120"/>
                    <DataGridTextColumn Header="Giờ Vào" Binding="{Binding CheckInTime, StringFormat=HH:mm}" Width="100"/>
                    <DataGridTextColumn Header="Giờ Ra" Binding="{Binding CheckOutTime, StringFormat=HH:mm}" Width="100"/>
                    <DataGridTextColumn Header="Trạng Thái" Binding="{Binding Status}" Width="100"/>
                    <DataGridTextColumn Header="Giờ Hành Chính" Binding="{Binding AdminHours}" Width="120"/>
                    <DataGridTextColumn Header="Giờ Tăng Ca" Binding="{Binding OvertimeHours}" Width="120"/>
                </DataGrid.Columns>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
        </ScrollViewer>
    </Grid>
</UserControl>